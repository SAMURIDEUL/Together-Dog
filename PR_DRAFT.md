## 📌 변경 사항 개요

메인 홈 화면에 사용될 `PlaceInfoCard` 컴포넌트를 구현하고, UI 패키지의 의존성을 정리했습니다. 또한 사용자 경험을 위해 스크롤바 스타일을 개선했습니다.

## 📝 상세 내용

### 1. `PlaceInfoCard` 구현

- **내용**:
  - `next/image` 최적화를 위해 메인 웹 앱으로 컴포넌트 이동
  - 이미지 비율을 `16:9`로 조정
  - 뱃지 리스트 가로 스크롤 구현 (커스텀 스크롤바 적용)
  - `LikeButton` 및 `Badge` 컴포넌트 연동

### 2. UI 패키지 리팩토링

- **의존성 제거**: `next` 패키지 의존성을 제거하여 순수 React 컴포넌트 라이브러리로 전환
- **컴포넌트 추출**: `PlaceInfoCard` 내부에 있던 좋아요 버튼을 `LikeButton` 컴포넌트로 분리하여 재사용성 증대
- **전역 스타일 정리**: 라이브러리 차원의 불필요한 전역 스크롤바 스타일 제거

### 3. 스타일링 개선

- **커스텀 스크롤바**: `.thin-scrollbar` 유틸리티 추가
  - 얇은 두께 (4px)
  - 투명한 트랙 배경
  - 무난한 회색 (`#D1D5DB`) 썸(Thumb) 적용
- **레이아웃 수정**: 스타일 import 경로 수정

### 📸 스크린샷

|      PlaceInfoCard 구현 (스크롤바 적용)      |
| :------------------------------------------: |
| ![스크롤바 확인](이미지를_여기에_넣어주세요) |

### ✅ 테스트 방법

1. 메인 웹 앱 디렉토리에서 개발 서버 실행
2. 테스트 페이지(`/test/card`) 접속
3. 카드가 정상적으로 렌더링되는지 확인
4. 뱃지 리스트를 가로로 스크롤하여 회색의 얇은 스크롤바가 나타나는지 확인

### 📋 리뷰 포인트

- UI 패키지에서 `next` 의존성이 완전히 제거되었는지 확인 부탁드립니다.
- `PlaceInfoCard`가 정상적으로 동작하는지 확인 부탁드립니다.
