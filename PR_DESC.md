## 📌 변경 사항 개요

프론트엔드의 **전체 API 연동 작업**을 완료했습니다.
스웨거(Swagger) 명세에 맞춰 Auth, User, Place, Review, Category 도메인의 모든 API 함수와 타입을 구현했습니다.

## 📝 상세 내용

### 1. API 함수 및 타입 구현 (`src/api/*`, `src/types/*`)

백엔드 API 스펙과 1:1로 매칭되는 요청 함수와 타입을 정의했습니다.

- **Auth (인증)**: 로그인, 회원가입, 로그아웃, 토큰 갱신, 이메일/닉네임 중복 확인
- **User (유저)**: 내 정보 조회/수정, 비밀번호 변경, 회원 탈퇴
- **Place (장소)**: 카테고리별 장소 목록 조회, 장소 상세 조회, 랜덤 장소 추천
- **Category (카테고리)**: 전체 카테고리 목록 조회
- **Review (리뷰)**:
  - 리뷰 작성 및 수정 (이미지 업로드를 위한 `multipart/form-data` 지원)
  - 리뷰 삭제
  - 내 리뷰 목록 조회
  - 특정 장소의 리뷰 목록 조회

### 2. API 응답 타입 표준화 (`src/types/api.ts`)

- 백엔드의 공통 응답 포맷(`ApiResponse<T>`)에 맞춰 제네릭 타입을 정의하여, 모든 API 호출에서 일관된 타입 추론이 가능하도록 했습니다.

### 3. 기타 개선 사항

- **PlaceInfoCard**: 카테고리 이미지 렌더링 스타일 개선 (`object-contain`) 및 호버 효과 추가
- **Mapper**: `categoryMapper`, `petMapper` 유틸리티 함수 최적화
- **Lint**: 미사용 변수 및 빈 인터페이스 관련 린트 에러 수정 완료

## 🔗 관련 이슈

Resolves: #30

## 💡 참고 사항

- **장소 목록 조회 API (`GET /categories/{id}/places`)**는 반환되는 데이터가 **'장소(Place)'**가 주체이므로, `api/category.ts`가 아닌 **`api/place.ts`**의 `getPlaces` 함수로 구현했습니다.
- 모든 API 함수에는 **한글 주석**을 달아두었으니(예: `// 내 정보 조회`), 사용 시 참고 부탁드립니다.
- 로컬 환경에서 테스트 스크립트를 통해 주요 API(카테고리, 중복확인 등)의 정상 응답을 검증했습니다.
